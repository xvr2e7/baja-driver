{
  "name": "baja-driver",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "To build:  initialize nod.js project by running: npm init -y install threejs by running: npm install --save three install vite by running: npm install --save-dev vite To run: npx vite",
  "dependencies": {
    "three": "^0.181.1"
  },
  "devDependencies": {
    "vite": "^6.4.1"
  }
}
